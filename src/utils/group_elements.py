from itertools import groupby

def group_elements(ungrouped_elements):
    print("Grouping...")

    # key_function = lambda element: element[""]
    # grouped_elements = groupby(ungrouped_elements, "frame")
    # print(grouped_elements)

    # for key, group in groupby(ungrouped_elements, "frame"):
    #     print(key + " :" + list(group))

    # print(ungrouped_elements[0].frames[0])
    # return grouped_elements
